#! /usr/bin/env python
# encoding: utf-8

def build(bld):
    bld.shlib( use = "BOOST core-container-serializer", target = 'ActivityFileWriter', source = 'ActivityFileWriter.cpp' )

    bld.recurse(['test'], mandatory=True)


# -*- indent-tabs-mode: t -*-
