/**
 * Test: Multiplexer: Chained Multiplexers
 *
 * Situation:
 * Multiplexer may be chained (across multiple systems). In such a setup however 
 * we loose a strict order of events. 
 * (only the order within a single system can be preserved)
 *
 * Assert:
 * ?
 * Output is also async?
 */
