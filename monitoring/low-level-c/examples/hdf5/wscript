#! /usr/bin/env python
# encoding: utf-8

import os

def build(bld):
   pass
   try:
   		os.symlink(bld.path.abspath() + "/siox.conf", "build/monitoring/low-level-c/examples/hdf5/siox.conf")
   except OSError, e:
   	 	pass
   #bld.program(includes="",  use = "siox-monitoring-siox-ll hdf5 hdf5_hl",  features='test c cprogram', target = 'hdf5-example', source = 'hdf5-example.c', install_path = None)


# -*- indent-tabs-mode: t -*-
