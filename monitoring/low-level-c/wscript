#! /usr/bin/env python
# encoding: utf-8


def build(bld):
    bld.shlib(includes="include",  target = "siox-ll", use = "module-loader AutoConfigurator", lib=['pthread', 'rt'], source = 'siox-ll.cpp' )
    bld.recurse(['test'], mandatory=True)


# -*- indent-tabs-mode: t -*-
