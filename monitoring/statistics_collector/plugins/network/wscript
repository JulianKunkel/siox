#! /usr/bin/env python
# encoding: utf-8

def build(bld):
    bld.shlib(includes="",  use = "siox-core-AutoConfigurator BOOST", target = 'siox-monitoring-statisticsPlugin-network', source = 'network.cpp' )

    bld.recurse(['test'], mandatory=True)


# -*- indent-tabs-mode: t -*-
