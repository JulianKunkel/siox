#! /usr/bin/env python
# encoding: utf-8

def build(bld):
	pass
    #bld.shlib(includes="include", use="BOOST core-container-serializer AutoConfigurator AutoRegistrar", target='ThreadedStatisticsCollector', source='ThreadedStatisticsCollector.cpp' )

    #bld.recurse(['plugins'], mandatory=True)


# -*- indent-tabs-mode: t -*-
